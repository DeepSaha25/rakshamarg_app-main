services:
  - type: web
    name: rakshamarg-backend
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: APP_API_KEY
        sync: false
      - key: GOOGLE_MAPS_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: NIRBHAYA_SERVICE_URL
        sync: false

  - type: web
    name: rakshamarg-chatbot
    runtime: python
    rootDir: nirbhaya_bot
    buildCommand: python -m pip install --upgrade pip setuptools wheel && python -m pip install --no-cache-dir --retries 10 --timeout 120 -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: PIP_DEFAULT_TIMEOUT
        value: 120
      - key: PIP_DISABLE_PIP_VERSION_CHECK
        value: 1
      - key: API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: LLM_PROVIDER
        value: gemini
